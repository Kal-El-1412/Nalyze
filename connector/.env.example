# CloakSheets Connector Configuration

# AI Mode (default: off)
# Set to "on" to enable AI-powered chat functionality
# When AI_MODE=off, the connector runs without requiring OpenAI API key
AI_MODE=off

# OpenAI API Key (REQUIRED when AI_MODE=on)
# Get your API key from: https://platform.openai.com/api-keys
# Leave commented out if AI_MODE=off
# OPENAI_API_KEY=sk-your-key-here

# Privacy Note:
# The OpenAI API only receives schema and aggregated results, never raw data rows.
# All data processing happens locally in ~/.cloaksheets/
